export const px = (value: number) => {
  return `${value}px`
}

export const translate = (x: string, y: string) => {
  return `translate(${x}, ${y})`
}

export class a {
  checked = 'Evet';
  unchecked = 'Hayır';
}

export class b {
  checked = '#00FF00'
  unchecked = '#FF0000'
  disabled = '#CCCCCC'
}
